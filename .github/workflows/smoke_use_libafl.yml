name: LibAFL smoke

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  smoke-use-libafl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go (bootstrap)
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"

      - name: Export GOROOT_BOOTSTRAP
        run: echo "GOROOT_BOOTSTRAP=$(go env GOROOT)" >> "$GITHUB_ENV"

      - name: Set up Rust (nightly)
        uses: dtolnay/rust-toolchain@nightly

      - name: Set up Java (Temurin 17)
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Install Grammarinator (pip)
        run: python3 -m pip install --user grammarinator

      - name: Build gosentry toolchain
        run: cd src && ./make.bash

      - name: Smoke (panic)
        run: bash misc/gosentry/tests/smoke_use_libafl_panic.sh

      - name: Smoke (overflow)
        run: bash misc/gosentry/tests/smoke_use_libafl_overflow.sh

      - name: Smoke (multiargs)
        run: bash misc/gosentry/tests/smoke_use_libafl_multiargs.sh

      - name: Smoke (multiparams)
        run: bash misc/gosentry/tests/smoke_use_libafl_multiparams.sh

      - name: Smoke (composite)
        run: bash misc/gosentry/tests/smoke_use_libafl_composite.sh

      - name: Smoke (reverse)
        run: bash misc/gosentry/tests/smoke_use_libafl_reverse.sh

      - name: Smoke (config)
        run: bash misc/gosentry/tests/smoke_use_libafl_config.sh

      - name: Smoke (focus-on-new-code)
        run: bash misc/gosentry/tests/smoke_use_libafl_focus_on_new_code.sh

      - name: Smoke (addseed + testdata/fuzz)
        run: bash misc/gosentry/tests/smoke_use_libafl_addseed_testdata.sh

      - name: Smoke (grammar json)
        run: bash misc/gosentry/tests/smoke_use_libafl_grammar_json.sh

      - name: Smoke (generate coverage)
        run: bash misc/gosentry/tests/smoke_use_libafl_generate_coverage.sh

  smoke-panic-on:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go (bootstrap)
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"

      - name: Export GOROOT_BOOTSTRAP
        run: echo "GOROOT_BOOTSTRAP=$(go env GOROOT)" >> "$GITHUB_ENV"

      - name: Set up Rust (nightly)
        uses: dtolnay/rust-toolchain@nightly

      - name: Build gosentry toolchain
        run: cd src && ./make.bash

      - name: Smoke (--panic-on)
        run: bash misc/gosentry/tests/smoke_use_libafl_panic_on.sh
